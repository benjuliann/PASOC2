[phases.setup]
node_version = "22.x"

[phases.install]
# Install dependencies
install = "npm ci"

[phases.build]
# Build the app
build = "npm run build"

[phases.start]
# Start the app
start = "PORT=3000 npm start"
port = 3000
